{
  "name": "open-file-dialog",
  "description": "Opens the browsers file dialog",
  "repository": "f-mer/open-file-dialog",
  "version": "1.0.1",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps"
  },
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "standard": "^10.0.2"
  },
  "keywords": [
    "browser",
    "file-dialog"
  ],
  "license": "MIT"
}
